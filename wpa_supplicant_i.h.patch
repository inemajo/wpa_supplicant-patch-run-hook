--- wpa_supplicant/wpa_supplicant_i.h.old	2013-05-02 13:16:53.638444122 +0200
+++ wpa_supplicant/wpa_supplicant_i.h	2013-05-02 13:18:50.915977951 +0200
@@ -183,6 +183,19 @@
 };
 
 /**
+ * struct wpas_exec - Internal data for wpa_supplicant event call
+ *
+ * This structure contains the internal data for the event call
+ * interface of wpa_supplicant when it execve (argv & environ)
+ */
+struct wpa_exec {
+  char		**env;
+  size_t	wpas_env_size;
+  char		*argv[2];
+};
+
+
+/**
  * struct wpa_global - Internal, global data for all %wpa_supplicant interfaces
  *
  * This structure is initialized by calling wpa_supplicant_init() when starting
@@ -196,6 +209,7 @@
 	void **drv_priv;
 	size_t drv_count;
 	struct os_time suspend_time;
+	struct wpa_exec	event_exec;
 };
 
 
